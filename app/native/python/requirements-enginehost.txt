# Dependencies for the Trinity Python engine host (mirrors backend/requirements.txt).
# The release installer vendors a pre-built environment; developers can install
# this file into any Python >= 3.11 virtual environment.
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.6
sympy>=1.12
numpy>=1.26
pytest>=8.0
httpx>=0.27
