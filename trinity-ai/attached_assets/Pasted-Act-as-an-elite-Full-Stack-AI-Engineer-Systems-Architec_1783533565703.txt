Act as an elite Full-Stack AI Engineer, Systems Architect, and Robotics/CAD Expert. 

Your objective is to take the existing repository at `https://github.com/siddhpararudra2-debug/trinity-ai.git` and transform it into "Trinity", the ultimate unified AI Engineering and Research Operating System. 

You must use the existing codebase as your foundation. Do not start from scratch. First, fix the existing architectural gaps in the repo, and then build out the advanced engines on top of it.

### PHASE 0: FIX EXISTING REPO GAPS
Before adding new features, you must fix the critical issues in the existing `trinity-ai` repo:
1. Port Standardization: Ensure the backend (`troyy_server.py`) and all frontend components strictly use port `8100`.
2. Health Check: Update `/api/health` in `troyy_server.py` to actually check if the DB and core routers loaded, returning "unhealthy" if they failed.
3. Missing Endpoints: Implement the missing `GET /projects` endpoint in `troyy.ai/api/routes.py` so the autonomy operator can list projects.
4. Launcher Integration: Update `launch_trinity.py` to automatically start the Next.js frontend (`troyy.ai/frontend`) alongside the backend.

### THE TRINITY ENGINE REGISTRY
Once the base repo is fixed, you must implement and integrate the following specialized engines via tool-calling:

1. Trinity Math Engine: Symbolic solving (SymPy), dimensional analysis (Pint), secure sandbox execution.
2. Trinity Quantum Lab: Circuit simulation (Qiskit), open dynamics (QuTiP), 3D Bloch sphere visualization.
3. Trinity Literature RAG: Vector search (ChromaDB), Knowledge Graph (NetworkX), arXiv ingestion.
4. Trinity Vision & Publishing: Handwritten OCR (pix2tex), publication plotting (Matplotlib), LaTeX/PDF export.
5. Trinity Collab Desktop: Real-time WebSocket notebooks for multi-user research.
6. Trinity Maker Engine (Mechanical): Generates Fusion 360 Python API scripts to automatically design 3D mechanical parts.
7. Trinity Maker Engine (PCB): Generates KiCad S-expression files (.kicad_sch, .kicad_pcb) to automatically route PCBs.
8. Trinity Autonomous Orchestrator: A ReAct loop that chains these tools together for self-driving research.

### THE UNIFIED CHATBOT DIRECTIVE
- Identity: The bot's name is Trinity. It is an elite research and engineering collaborator.
- Interface: A single chat window in the Next.js frontend. The UI dynamically renders widgets based on the engine used (e.g., LaTeX blocks for Math, 3D canvases for Quantum, Download links for Maker CAD/PCB).
- Orchestration: The backend uses a Unified Router. When a user asks "Design a 4-layer PCB for an ESP32", the Router uses the Maker Engine (KiCad) to generate the files and returns a unified response.

### TECHNICAL STACK CONSTRAINTS
- Backend: FastAPI, Python, LiteLLM (for model routing), SQLite, ChromaDB.
- Frontend: Next.js 14 (App Router), TailwindCSS, React Three Fiber (for 3D), WebSockets.
- CAD/PCB: Fusion 360 Python API script generation, KiCad S-expression generation.

### YOUR TASK: EXECUTION STEPS
Since the entire codebase is massive, you must execute this in the following structured steps. Do not use placeholder stubs; write actual, working logic.

**STEP 1: Repo Initialization & Gap Fixing**
Provide the exact terminal commands to clone the repo. Then, provide the exact Python code to fix the 4 critical gaps mentioned in PHASE 0 (specifically the updated `troyy_server.py` health check, the `GET /projects` route, and the updated `launch_trinity.py`).

**STEP 2: Master Directory Structure**
Provide a complete ASCII tree of the updated project structure, showing exactly where the new `trinity/maker_engine/`, `trinity/quantum_engine/`, `trinity/math_engine/`, and `trinity/unified_router/` directories belong inside the existing repo.

**STEP 3: The Unified Trinity Router (Backend Brain)**
Write the complete, production-ready Python code for `trinity/unified_router/orchestrator.py`. This is the brain. It must use LiteLLM to analyze the user's prompt, decide which of the 8 engines to trigger, extract parameters, and execute the tool. Include the exact system prompt it uses to make routing decisions.

**STEP 4: The Trinity Maker Engine (CAD & PCB)**
Write the complete Python code for:
- `trinity/maker_engine/fusion_generator.py`: A class that takes natural language intent and generates a valid Fusion 360 Python script to create a parametric L-bracket.
- `trinity/maker_engine/kicad_generator.py`: A class that takes natural language intent and generates valid KiCad `.kicad_sch` and `.kicad_pcb` S-expression files for an ESP32 board.

**STEP 5: The Core Science Engines (Math & Quantum)**
Write the complete Python code for:
- `trinity/math_engine/core.py`: The SymPy wrapper for symbolic solving.
- `trinity/quantum_engine/circuit_sim.py`: The Qiskit wrapper that simulates circuits and extracts Bloch sphere vectors.

**STEP 6: The Unified Chat UI (Frontend)**
Write the complete Next.js (App Router) code for the single chat interface (`app/page.tsx` and `components/TrinityChat.tsx`). It must:
- Send messages to the backend router.
- Parse the JSON response to see which `engine` was triggered.
- Dynamically render specific UI widgets (e.g., if `engine == "maker_pcb"`, show a "Download KiCad Files" button; if `engine == "math"`, render LaTeX using `react-katex`; if `engine == "quantum"`, render the 3D Bloch sphere using React Three Fiber).

**STEP 7: Final FastAPI Integration**
Write the updated `troyy_server.py` file that initializes FastAPI, includes all the new routers (Maker, Math, Quantum, Unified Router), and sets up the WebSocket endpoint for the Collab engine.

Begin executing these steps now. Write clean, modular, and heavily commented code. Ensure all imports are correct and the code is ready to run.