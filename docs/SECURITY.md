# Security

Artifacts receive server-generated IDs and paths, SHA-256 checksums, and are copied from engine scratch space into managed storage. Adapter and scaffold capability errors never fall back to fake files. Generated code must execute only in a restricted subprocess in a future firmware implementation.
