# CAD

The flagship `quadcopter_frame` supports a 50 mm four-motor X frame and produces valid binary STL, GLB 2.0, and JSON specification artifacts. `step` is reported unavailable unless a real CAD kernel is installed; Trinity never writes fake STEP files.
